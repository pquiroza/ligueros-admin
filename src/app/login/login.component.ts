import { Component } from '@angular/core';


@Component({
  selector: 'ngx-login',
  styleUrls: ['login.component.scss'],
  template: `
    <login-layout>

      <router-outlet>
      </router-outlet>
    </login-layout>
  `,
})
export class LoginComponent {















}
